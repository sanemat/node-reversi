'use strict';

var reversi = require('../');
var assert = require('power-assert');

describe('reversi', function () {
  it('returns', function () {
    console.log(reversi.makeInitialGameBoard());
  });
});
